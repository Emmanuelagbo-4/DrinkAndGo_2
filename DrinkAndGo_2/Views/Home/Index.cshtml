@model HomeViewModel
@await  Html.PartialAsync("Carousel")

<h2>Prefered Drinks of the week</h2>
<h4>Client Prefrences - just for you!</h4>

<div class="row">
    @foreach (var drink in Model.PreferredDrinks)
    {
        @await Html.PartialAsync("DrinkSummary", drink)
    }
</div>